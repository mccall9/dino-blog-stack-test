{
  "name": "dino-blog-stack-test",
  "private": true,
  "description": "Experimental stack rewrite of dino.blog for Vercel preview. NOT production. Production stays static on dinoclub.blog (DM Sans).",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:api": "bun run --filter @dino-blog/api dev",
    "dev:web": "bun run --filter @dino-blog/web dev",
    "build": "turbo run build",
    "build:vercel": "cd apps/web && bun run build && cd ../.. && node scripts/copy-vercel-output.mjs",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.10.5",
    "typescript": "^5.8.3"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.1.0"
  }
}
